    <div class="row">
        <div class="col-xs-12 col-sm-6 col-md-6 pl-0">
            <a class="navbar-brand" href="{{ url('/elite_individual') }}" >
                <img src="{{ asset('/images/loader/ELITE-top-white.png') }}" alt="Elite Individaul"/>
            </a>
        </div>
        <div class="col-xs-12 col-sm-12 col-md-6 pr-0" style="{{ isTablet ? 'padding-right: 70px;' : null }}">
            <p class="loggedin">
                <span>{{ user.getName() }}</span>
                <span class="color-dark-grey">{{ user.getCompany().company_name }}</span>
                {% if not isPhone %}
                <span>
                    <a href="{{ url('/elite_individual') }}">HOME</a>
                </span>
                {% endif %}
                <span>
                    <a href="{{ url('/user/logout') }}">LOGOUT</a>
                </span>
            </p>
        </div>
    </div>