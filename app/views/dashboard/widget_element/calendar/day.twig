<div
    class="calendar-date
    {{ day.disabled ? 'is-disabled' : null }}
    {{ day.tooltip ? 'tooltip' : null }}"
    data-tooltip="{{ day.tooltip ? day.tooltip : null }}"
>
    <button class="date-item">{{ day.index }}</button>
</div>