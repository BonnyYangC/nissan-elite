<div class="table-responsive">
    <table class="table centre-text">
        <tr class="nissangray-back">
            <td colspan="2">FY19 i_ELITE</td>
        </tr>
        <tr class="nissangray-light-back">
            <td>
                <form action="{{ env('lastYear') }}/../admin/fake-user" method="post">
                    <input type="hidden" name="uid" value="{{ user.old_id }}" />
                    {% if dashboardMenuOnly %}
                        <input type="hidden" name="mock" value="true" />
                    {% endif %}
                    <button style="background-color:#4169E1; color: #FFFFFF; width:100%; margin:0 auto; font-weight: lighter; font-size:11px;" type="submit" class="btn btn-primary btn-block">
                        View Previous Year
                    </button>
                </form> 
            </td>
        </tr>
    </table>
</div>
