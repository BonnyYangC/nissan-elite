{% extends 'layout/frontend.twig' %}
{% block content %}
   <div class="container bg-white">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">My ACCOUNT</h1>
            </div>
        </div>
        <div class="row pb-20">
            <div class="col-md-9 mb-200">
                <h3>Personal Details</h3>
                <div class="table-responsive">
                    <table class="table">
                        <tr class="table-top-row">
                            <td><strong>Details</strong></td>
                            <td><strong>{{ constant('ProgramShortName') }} Program Member</strong></td>
                        </tr>

                        <tr class="active">
                            <td><strong>Registration Number</strong></td>
                            <td>{{ user.employee_code }}</td>
                        </tr>
                        <tr>
                            <td><strong>Title</strong></td>
                            <td>{{ user.salutation }}</td>
                        </tr>
                        <tr class="active">
                            <td><strong>First</strong></td>
                            <td>{{ user.firstname }}</td>
                        </tr>
                        <tr>
                            <td><strong>Surname</strong></td>
                            <td> {{ user.lastname }}</td>
                        </tr>
                        <tr class="active">
                            <td><strong>Date of Birth</strong></td>
                            <td>{{ user.dob|split('-')|reverse()|join('-') }}</td>
                        </tr>
                        <tr class="">
                            <td><strong>Date Created</strong></td>
                            <td>{{ user.date_created|split('-')|reverse()|join('-') }}</td>
                        </tr>
                        <tr class="active">
                            <td><strong>Mobile</strong></td>
                            <td>{{ user.mobile }}</td>
                        </tr>
                        <tr class="">
                            <td><strong>Email Address</strong></td>
                            <td>{{ user.email }}</td>
                        </tr>
                        <tr class="active">
                            <td><strong>Dealership</strong></td>
                            <td>{{ user.company_name }}</td>
                        </tr>
                        <tr class="">
                            <td><strong>Dealer Code</strong></td>
                            <td>{{ user.company_code }}</td>
                        </tr>
                        <tr class="active">
                            <td><strong>Department</strong></td>
                            <td>{{ user.department_name }}</td>
                        </tr>
                        <tr class="">
                            <td><strong>Position</strong></td>
                            <td>{{ user.position_desc }}</td>
                        </tr>
                    </table>
                    <p>
                        The details above are currently held on the Nissan Dealer User Management Portal. Please check they are correct. If not, please update, ask your Dealership to update, or email <a href="mailto:{{ env('SUPPORT_EMAIL_ADDRESS') }}">{{ env('SUPPORT_EMAIL_ADDRESS') }}</a> immediately. Incorrect details may mean your {{ constant('ProgramShortName') }}{{ (env('YEAR')~'-01-01')|date('y') }} credits are incorrect<br>
                    </p>
                </div>
            </div>
       </div>
   </div>
{% endblock %}