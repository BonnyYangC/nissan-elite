{% extends 'layout/frontend.twig' %}
{% block content %}
    <script>
        {#var NEW_VEHICLE_SALES = [{{ metrics.NEW_VEHICLE_SALES|raw }}];#}
        {#var SALES_RECOMMENDATION = [{{ metrics.SALES_RECOMMENDATION|raw }}];#}
        {#var FOLLOW_UP_CREDITS = [{{ metrics.FOLLOW_UP_CREDITS|raw }}];#}
        var TRAINING = [{{ metrics.TRAINING|raw }}];
    </script>
    <div class="container bg-white pl-25">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    <a class="print-icon" href="#" onclick="window.print();return false;">
                        <i class="fa fa-print" aria-hidden="true" title="Print"></i>
                    </a>
                    <br>
                    MY Metrics - <em>Sales Consultant</em>
                </h1>
            </div>
        </div>
        <div class="row pb-20 pl-10">
            <div class="col-md-12 col-sm-12  pb-20 pl-10">
                {% include 'dashboard/metrics/elements/new_vehicle_sales.twig' with {'index':1} %}
                {% include 'dashboard/metrics/elements/sales_recommendation.twig' with {'index':2} %}
                {% include 'dashboard/metrics/elements/follow_up_credits.twig' with {'index':3} %}
                {% include 'dashboard/metrics/elements/training.twig' with {'index':4} %}
            </div>
        </div>
    </div>
{% endblock %}