{% extends 'layout/frontend.twig' %}
{% block content %}
<div class="container bg-white pl-25 pr-25">
    <div class="ac-tooltip">1000</div>
    <div class="row">
        <div class="col-md-9">
            <h1 class="page-header" > <span style="font-size:20px; padding-top:0;">Welcome {{ user.firstname }}</span></h1>
        </div>
    </div>
    <!-- Projects Row -->
    <div class="row">
        <div class="col-md-9">
            <div class="table-responsive">
                {% set chartInitData = {'title': 'Monthly Credits','el_id':'bar-chart'} %}
                {% include 'dashboard/widget_element/bar-chart.twig' with chartInitData %}
            </div>
            <hr>
            <div class="row">
                <div class="col-md-6 portfolio-item">
                    <h3>LEADER BOARD</h3>
                    {% include 'dashboard/widget_element/leader_board_table.twig' %}
                </div>

                <div class="col-md-6">
                    <h3>Current STATUS LEVEL</h3>
                    {% include 'dashboard/widget_element/current_status_level_chart.twig' %}
                </div>
            </div>
            <hr>
            <div class="row">
                <div class="col-md-12">
                    <!---- Sales Percentage---->
                    <h3><a href="{METRICS-IND-RESULTS-LINK}">Credits earned per Month (View Individual Results)</a></h3>
                    {MULTIPLE-ROLE-SELECT}
                    <!-- <div style="padding: 10px 0;">
                      <strong>You have had multiple roles: </strong>
                      <select id="metricsRoleSelect">
                        <option value="nissan_salesconsultants">Sales Consultant</option>
                        <option value="nissan_salesmanagers">Sales Manager</option>
                      </select>
                    </div> -->
                    <script>
                        // var metricsRoleSelect = document.getElementById('metricsRoleSelect');
                        //
                        // if (metricsRoleSelect) {
                        //     metricsRoleSelect.addEventListener('change', function (e) {
                        //         window.location.href = '/Dashboard/' + e.target.value;
                        //     });
                        // }
                    </script>
                    <div class="table-responsive" >
                        <div style="width: 95%">
                            <canvas id="canvas"></canvas>
                        </div>
                        <script>
                            // var barChartData = {
                            //     labels: ["April", "May", "June", "July", "August", "September", "October", "November", "December" , "January", "February", "March"],
                            //     datasets: [{METRICS}]
                            //
                            // };
                            // window.onload = function() {
                            //     var ctx = document.getElementById("canvas").getContext("2d");
                            //     window.myBar = new Chart(ctx, {
                            //         type: 'bar',
                            //         data: barChartData,
                            //         options: {
                            //             tooltips: {
                            //                 mode: 'index',
                            //                 intersect: false
                            //             },
                            //             responsive: true,
                            //             scales: {
                            //                 xAxes: [{
                            //                     stacked: true,
                            //                 }],
                            //                 yAxes: [{
                            //                     stacked: true
                            //                 }]
                            //             }
                            //         }
                            //     });
                            // };
                            //
                            // document.getElementById('randomizeData').addEventListener('click', function() {
                            //     barChartData.datasets.forEach(function(dataset, i) {
                            //         dataset.data = dataset.data.map(function() {
                            //             return randomScalingFactor();
                            //         });
                            //     });
                            //     window.myBar.update();
                            // });
                        </script>
                        <br>
                        <p>On completion of your annual registration credits will be applied in same month.</p>
                        <p>Credits will also be applied in same month for dealer excellence if your dealer achieved in the previous year.</p>
                    </div>
                    <hr>
                </div>
            </div>
        </div>
        <div class="col-md-3 pb-0">
            <h3>RANKINGS &amp; CREDITS</h3>
            {% include 'dashboard/widget_element/current_ranking_table.twig' %}
            {% include 'dashboard/widget_element/year_to_date_table.twig' %}
            {% include 'dashboard/widget_element/life_time_table.twig' %}
            {% include 'dashboard/widget_element/are_you_registered_table.twig' %}
            {% include 'dashboard/widget_element/traing_or_track_table.twig' %}
            {% include 'dashboard/widget_element/dealer_excellence_table.twig' %}
            {% include 'dashboard/widget_element/events_calendar.twig' %}
        </div>
    </div>
</div>
{% endblock %}